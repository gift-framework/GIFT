# Phase 3.S1: residual-source certificate for `C_src`

## Status

Decomposition fixed; D0 sharp-current certificate completed.

This file isolates the exact remaining work needed to upgrade

`||m(h_bar_global)||_{Y_-3} <= C_src * r0^(7/2) * ||A_bulk||_{C^{0,alpha}} + C_aff * eta_aff`

from a correct structural estimate into a theorem-grade certified bound.

Current D0 certificate:

- source coefficient `C_src = 27/16` from the private P1 chain;
- `R_aff(D0) = 0`;
- `R_comm` controlled as a clean moving-coordinate parametrix factor
  `q_comm <= 0.68`, not as an additive source envelope.

Public machine artifact:

- [phase3_projection_commutator_certificate.json](../certificates/phase3_projection_commutator_certificate.json).

## The three source pieces

The residual source splits as:

`m(h_bar_global) = R_quad + R_comm + R_aff`.

The machine-readable freeze of this decomposition is:

- [phase3_source_residual_ledger.json](../certificates/phase3_source_residual_ledger.json).

## `R_quad`: collar quadratic remainder

This is the leading source term from the branched jet on each collar.

Structural content already fixed:

- the local seed is `h_loc,aff + h_loc,branch`,
- the affine part is exactly maximal,
- the linearized contribution vanishes at the relevant lower trace,
- the first nonzero source therefore comes from the quadratic self-interaction of
  `c0 * w^(3/2) * alpha_1`.

The draft records the leading form as:

`R_quad ~ |c0|^2 * A_bulk(alpha_1, alpha_1) * rho^(1/2)`

in the `m = +/- 1/2` sigma-odd channel.

2026-07-02 update:

- the old quadratic reading is superseded for the fixed-Sigma obstruction
  channel;
- the sharp-current sigma-odd source is cubic in `c0`;
- the theorem-grade coefficient is `C_src = 27/16`;
- the `Y_-3` contribution at D0 is `1.6875e-7`.

The first explicit coefficient scaffold is now frozen in:

- [phase3_Rquad_coefficient.md](phase3_Rquad_coefficient.md),
- [phase3_Rquad_coefficient_scaffold.json](../certificates/phase3_Rquad_coefficient_scaffold.json).

The corresponding local sanity check showing what the flat toy model does *not*
capture is frozen in:

- [phase3_branch_seed_sanity.md](phase3_branch_seed_sanity.md),
- [phase3_branch_seed_sanity.json](../certificates/phase3_branch_seed_sanity.json).

The exact coefficient already available in the correct lower-root channel is
frozen separately in:

- [phase3_mu_minus_half_channel.md](phase3_mu_minus_half_channel.md),
- [phase3_mu_minus_half_channel.json](../certificates/phase3_mu_minus_half_channel.json).

## `R_comm`: annular cutoff commutator

This is the source generated by gluing the collar and bulk pieces.

Structural content already fixed:

- support is confined to the annular overlap,
- `[J_h, chi_i]` has the correct weight shift `beta -> beta-2`,
- the relevant norm is finite and depends only on cutoff geometry and the local elliptic constants.

What is still missing:

- integration of the clean commutator factor into the full Phase 3.2/3.3 global
  parametrix theorem.

The numerical local factor is now certified:

`q_comm = (4/3) K_Sch^Maz kappa_E epsilon`.

At D0, with `K_Sch^Maz <= 17`, `kappa_E <= 3`, `epsilon = 10^-2`:

- `q_comm <= 0.68 < 1`;
- `epsilon_0 >= 0.014705882352941176 > 10^-2`.

In the clean moving-coordinate construction, `R_comm` is an operator/parametrix
Neumann factor, not an additive source coefficient in `C_src`.

## `R_aff`: affine mismatch term

This is the only one of the three pieces that is already completely settled at `D0`.

For the normalized datum:

- `eta_aff(D0) = 0`,
- because the 77 components are identical copies of one local model.

Therefore:

`R_aff(D0) = 0`

exactly.

This is already safe to use as a theorem input specific to `D0`.

## What the old `C_src <= 2` meant

The old `G`-quant script used a single conservative placeholder:

`C_src <= 2`.

This was acceptable as a *conditional numerical closure* device, because it yields

`||m(h_bar_global)||_{Y_-3} <= 2e-7`

at `D0`, which is far below the contraction threshold once combined with the current linear bound.

It is now superseded at D0 by:

- `C_src = 27/16`;
- `||m(h_bar_global)||_{Y_-3} <= 1.6875e-7`;
- `R_comm` certified separately as `q_comm <= 0.68`.

## Minimal theorem-grade upgrade path

The remaining honest route is:

1. write the public theorem text integrating the private `C_src = 27/16`
   derivation;
2. integrate the public `Pi_obs` reduced-model certificate;
3. carry `q_comm <= 0.68` into the global parametrix theorem;
4. assemble the Phase 3.2/3.3 right inverse theorem.

## What becomes possible once `C_src` is certified

With this file updated, the source term is no longer the active D0 bottleneck.
The remaining Phase 3 work is global: operator assembly, full `Pi_obs`
identification, and the weighted mapping theorem in `X_beta^{ext}`.
